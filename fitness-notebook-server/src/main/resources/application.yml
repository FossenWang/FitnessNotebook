spring:
    profiles:
        active: dev,test
---
spring:
    profiles: prod
    datasource:
        url: jdbc:postgresql://47.100.166.183:5432/fitness
        username: fossen
        password: 4k5j6h
        driver-class-name: org.postgresql.Driver
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: none

---
spring:
    profiles: dev
---
spring:
    profiles: test
    datasource:
        url: jdbc:h2:mem:fossen
        username: fossen
        password: fossen
        driver-class-name: org.h2.Driver
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create-drop